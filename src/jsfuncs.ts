// sort function

// function sortItems<T>(items: T[], order: 'asc' | 'desc' = 'asc'): T[] {
//   return items.sort((a, b) => {
//     if (order === 'asc') {
//       return a < b ? -1 : 1
//     } else {
//       return a < b ? 1 : -1
//     }
//   })
// }
